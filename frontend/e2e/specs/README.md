# Test plans

Plans written by the `playwright-test-planner` agent, one Markdown file per area (for example `favorites.md`). Each names `e2e/seed.spec.ts` as its seed. The `playwright-test-generator` agent turns a plan into specs in `e2e/`.

Review a plan before generating from it; it's the cheapest place to fix what gets tested.
